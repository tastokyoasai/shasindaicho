# 変更履歴（写真台帳アプリ 統合版）

版数は git のタグ（例 `v2.23`）と対応させます。日付は目安です。

## v2.23
- 既定モデル順を **①gemini-3.5-flash → ②gemini-3.5-flash-lite → ③gemini-2.5-flash-lite** に変更。
- 中継サーバーで **503等の過負荷時に次モデルへ自動切替＋約0.7秒待って計2巡リトライ**（“全部エラー”の解消）。
- 3.x系への `thinkingBudget` 送信をやめ **400 BadRequest を解消**（思考オフは2.5系のみ）。

## v2.22
- 高精度OCRの既定モデル候補を **①gemini-3.5-flash-lite → ②gemini-2.5-flash-lite → ③gemini-3.5-flash** の順に変更（Lite優先でコスト・無料枠を最適化、404時フォールバック）。
- 使用モデル名を成功レスポンスの **`X-OCR-Model` ヘッダ**で返却（Networkで現行モデルを確認可能）。

## v2.21
- **作業の保存／復元をフォルダ連携化**：フォルダ取り込み時（選択 or ドラッグ＆ドロップ）に、「作業を保存」で写真フォルダ内へ `写真台帳_作業状態.json` を上書き保存。「作業を復元」でフォルダを選ぶだけで復元＋**未処理写真だけ差分解析**。フォルダに作業状態があれば取り込み時に自動で「復元しますか？」と確認。
- 状況(joukyou)を候補に寄せず**書いてあるとおり読み取り**。対象1/2の**自由記入（漢字等）にも対応**。思考オフ（`thinkingBudget:0`）で高速化。

## v2.2
- **統合デバッグ**（データ整合・安全性）：写真を相対パス(photoKey)で識別し同名ファイルの取り違えを防止／作業状態復元の写真二重計上・不正グループを防止／解析中断時に未解析写真を台帳へ混ぜない／マスタ・撮影者チップをDOM APIで生成（HTML注入対策）／CSVの数式インジェクション無害化／Excelは撮影日を日付セル化・印刷範囲を実データ行に修正／ファイル名のWindows安全化／コピー元サブフォルダの角括弧等を実名で辿る／`file://`でも同梱Tesseractを優先読込。
- 手書きモードの識別QRを内容ちょうど `T1` に限定。
- **中継サーバー強化**：`ALLOW_ORIGIN` 必須（未設定は全要求を拒否）・レート制限・リクエスト/画像サイズ上限・MIME検査・OCRヒントの許可リスト化。

## v2.1
- 手書きOCRの実行状況を「〇/〇枚完了」＋進捗バーに簡素化。
- 台帳「種別2」は対象写真の空欄で既定「調査」。
- 高精度クラウドOCRをデジタル看板モードでも利用可能に。

## v2.0（統合版）
- 「写真台帳 照合アプリ（デジタル看板）」と「手書き写し込み台帳アプリ（手書き看板）」を1本の `index.html` に統合。起動時にモード選択。モードに応じてQRの扱い（照合／OCR）とUIを自動で切り替え。CSV/Excel・保存復元・振り分け・マスタ編集は両モード共通。
